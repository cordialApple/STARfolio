# deps
node_modules/

# claude code local settings (personal, per-machine)
.claude/settings.local.json

# build output
app/out/
app/dist/
dist/
out/

# electron-builder / native rebuild artifacts
app/release/
*.node

# local models & runtime data (downloaded to userData at runtime; never committed)
app/models/
models/
app/.cache/

# recorded AI fixtures (may contain pasted user content; keep out of the repo)
**/ai-fixtures/
app/tests/fixtures/ai/

# logs & temp
*.log
.DS_Store
Thumbs.db

# env / secrets
.env
.env.*
!.env.example

# editor
.vscode/*
!.vscode/extensions.json
.idea/

# test artifacts
app/test-results/
app/playwright-report/
app/blob-report/
app/.playwright/
